# Pre-push hook - lightweight checks only
# Integration tests run via: npm run pr:create (before PR creation)

echo "ℹ️  Pre-push: Integration tests run locally before PR creation"
echo "   Use: npm run pr:create"
